# Core: algorithm and inference
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
opencv-python>=4.8.0

# Image matching (GitHub only — the PyPI package of the same name is unrelated)
vismatch @ git+https://github.com/gmberton/vismatch.git

# Fine-tuning (train.py)
lightglue @ git+https://github.com/cvg/LightGlue.git
scikit-learn>=1.3.0
tqdm>=4.65.0

# Dataset: access and satellite imagery I/O
kagglehub>=0.2.0
rasterio>=1.3.0
pandas>=2.0.0

# Notebooks: visualization and cloud filter comparison
matplotlib>=3.7.0
s2cloudless>=1.7.0
